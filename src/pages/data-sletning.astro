---
import Layout from "../layouts/Layout.astro";
import VideoBackground from "../components/ui/VideoBackground.astro";
import FadeIn from "../components/animations/FadeIn.astro";
import { Trash2, Mail, AlertCircle } from "lucide-astro";
---

<Layout title="Anmodning om datasletning" noindex={true}>
  <!-- Hero Section with Video Background -->
  <section class="relative min-h-[40vh] flex items-center justify-center overflow-hidden">
    <VideoBackground
      src="/videos/tech-background.mp4"
      overlay="heavy"
    />

    <div class="relative z-10 container mx-auto px-6 text-center">
      <FadeIn>
        <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary-500/20 mb-6">
          <Trash2 size={32} class="text-primary-500" />
        </div>
        <h1 class="text-4xl md:text-6xl font-extrabold tracking-tighter mb-4">
          Anmodning om <span class="bg-gradient-to-r from-primary-500 via-primary-600 to-primary-700 text-transparent bg-clip-text">Datasletning</span>
        </h1>
        <p class="text-xl text-slate-300 mt-2 max-w-2xl mx-auto">
          Her kan du anmode om sletning af din konto og tilknyttede data.
        </p>
      </FadeIn>
    </div>
  </section>

  <section class="container mx-auto px-6 py-20">
    <FadeIn>
      <div class="max-w-2xl mx-auto glass-card rounded-2xl p-8">
        <h2 class="text-2xl font-bold text-white mb-4 flex items-center gap-3">
          <Mail size={24} class="text-primary-500" />
          Sådan anmoder du om sletning
        </h2>
        <p class="mb-6 text-slate-300">
          For at anmode om sletning af din konto og dine personlige data, bedes du
          sende en e-mail til den angivne adresse. Dette link vil automatisk
          udfylde emnefeltet.
        </p>
        <div class="text-center">
          <a
            href="mailto:jens@rnsapps.dk?subject=Anmodning%20om%20sletning%20af%20brugerdata"
            class="inline-flex items-center gap-2 bg-gradient-to-r from-primary-500 to-primary-600 hover:shadow-lg hover:shadow-primary-500/30 hover:scale-105 text-white font-bold py-3 px-8 rounded-full transition-all duration-300"
          >
            <Mail size={20} />
            Send anmodning via e-mail
          </a>
        </div>

        <div class="mt-10 pt-8 border-t border-white/10">
          <h3 class="text-xl font-semibold text-white mb-4 flex items-center gap-3">
            <AlertCircle size={22} class="text-yellow-500" />
            Vigtig information
          </h3>
          <ul class="space-y-4 text-slate-400">
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary-500/20 text-primary-500 flex items-center justify-center text-sm font-bold">1</span>
              <span>Din anmodning skal tydeligt referere til dit brugernavn eller din e-mailadresse, som er tilknyttet din konto.</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary-500/20 text-primary-500 flex items-center justify-center text-sm font-bold">2</span>
              <span>Følgende data slettes: brugerprofil, aktivitetslog, indlæg og andre personlige oplysninger.</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary-500/20 text-primary-500 flex items-center justify-center text-sm font-bold">3</span>
              <span>Visse data, som f.eks. transaktionshistorik, kan blive opbevaret i en yderligere periode af juridiske eller bogføringsmæssige årsager.</span>
            </li>
          </ul>
        </div>
      </div>
    </FadeIn>
  </section>
</Layout>

<style>
  .glass-card {
    background: rgba(59, 59, 59, 0.5);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }
</style>
