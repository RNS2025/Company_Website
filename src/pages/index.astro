---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/sections/Hero.astro";
import ServicesGrid from "../components/sections/ServicesGrid.astro";
import ProcessTimeline from "../components/sections/ProcessTimeline.astro";
import StatsSection from "../components/sections/StatsSection.astro";
import ContactForm from "../components/forms/ContactForm.astro";
import ProjectCard from "../components/project/ProjectCard.astro";
import Section from "../components/ui/Section.astro";
import Button from "../components/ui/Button.astro";
import Icon from "../components/ui/Icon.astro";
import FadeIn from "../components/animations/FadeIn.astro";
import { projects } from "../data/projects";

// Get featured projects (the two SMASH projects)
const featuredProjects = projects.slice(0, 2);
---

<Layout title="Forside - RNS APPS">
  <!-- Hero Section -->
  <Hero />

  <!-- Services Grid -->
  <ServicesGrid />

  <!-- Featured Projects 
	<Section>
		<FadeIn>
			<div class="text-center mb-16">
				<h2 class="text-4xl md:text-5xl font-bold mb-4">
					Udvalgte <span class="bg-gradient-to-r from-primary-500 to-primary-600 text-transparent bg-clip-text">Projekter</span>
				</h2>
				<p class="text-xl text-gray-300 max-w-2xl mx-auto">
					Se nogle af vores nyeste og mest spændende projekter
				</p>
			</div>
		</FadeIn>

		<div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
			{featuredProjects.map((project) => (
				<ProjectCard
					title={project.title}
					description={project.tagline}
					image={project.thumbnail}
					tags={project.tags}
					link={`/projekter/${project.slug}`}
					featured={true}
				/>
			))}
		</div>

		<FadeIn>
			<div class="text-center">
				<Button href="/projekter/" variant="outline" size="lg">
					<Icon name="arrow-right" class="w-5 h-5" />
					Se Alle Projekter
				</Button>
			</div>
		</FadeIn>
	</Section>
    -->

  <!-- Process Timeline -->
  <ProcessTimeline />

  <!-- Stats Section 
  <StatsSection />
-->
  <!-- Contact Form -->
  <Section>
    <div class="max-w-3xl mx-auto">
      <ContactForm
        title="Klar til at Starte Dit Næste Projekt?"
        description="Fortæl os om din idé, og lad os sammen skabe noget fantastisk. Vi svarer normalt inden for 24 timer."
      />
    </div>
  </Section>
</Layout>
