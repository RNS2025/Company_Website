---
import { useTranslations } from '../../data/locales/i18n';

const locale = Astro.currentLocale || 'da';
const t = useTranslations(locale);
const { about } = t;
---

<section class="cinematic-station cinematic-about" id="om-os" data-cinematic-station>
  <div class="cinematic-about__inner">
    <p class="cinematic-about__label">{about.label}</p>
    <h2 class="cinematic-about__headline">{about.headline}</h2>
    <p class="cinematic-about__intro">{about.intro}</p>

    <div class="cinematic-about__grid">
      <div class="cinematic-about__philosophy">
        <h3>{about.philosophyTitle}</h3>
        <p>{about.philosophy}</p>
      </div>

      <div class="cinematic-about__team">
        <h3>{about.teamTitle}</h3>
        <div class="cinematic-about__members">
          {about.team.map((member) => (
            <article class="cinematic-about__member">
              <h4>{member.name}</h4>
              <p>{member.bio}</p>
            </article>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
