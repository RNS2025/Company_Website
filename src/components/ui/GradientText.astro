---
interface Props {
    as?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p' | 'span';
    class?: string;
}

const {
    as: Tag = 'span',
    class: className = ''
} = Astro.props;
---

<Tag class={`gradient-text ${className}`}>
    <slot />
</Tag>
